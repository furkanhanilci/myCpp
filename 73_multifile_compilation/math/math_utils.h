#ifndef MATH_UTILS   // if not defined
#define MATH_UTILS   // define math_utils

struct Rectangle
{
    double lenght;
    double width;
};

double area(double lenght, double width);

double area(double lenght);

double area(Rectangle rectangle);

double pow(double base, int pow = 2);
#endif // if statement'ı bitir.



// header file'da fonksiyonların deklerasyonları yapılır.